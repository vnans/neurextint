{% extends 'base.html.twig' %}

{% block title %}Viste{% endblock %}

{% block body %}
    <h1>Viste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ viste.id }}</td>
            </tr>
            <tr>
                <th>Moderechargement</th>
                <td>{{ viste.moderechargement }}</td>
            </tr>
            <tr>
                <th>Rattachement</th>
                <td>{{ viste.rattachement }}</td>
            </tr>
            <tr>
                <th>Verificationid</th>
                <td>{{ viste.verificationid }}</td>
            </tr>
            <tr>
                <th>Point</th>
                <td>{{ viste.point }}</td>
            </tr>
            <tr>
                <th>Bundle</th>
                <td>{{ viste.bundle }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ viste.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('viste_index') }}">back to list</a>

    <a href="{{ path('viste_edit', {'id': viste.id}) }}">edit</a>

    {{ include('viste/_delete_form.html.twig') }}
{% endblock %}
