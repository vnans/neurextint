<form method="post" action="{{ path('pos1_delete', {'id': pos1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pos1.id) }}">
    <button class="btn">Delete</button>
</form>
